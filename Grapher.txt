@name Grapher
@inputs EGP:wirelink GraphicalValuable:number
@persist Frequency:number Curframe:number TableValues:array

#[


comments and howto and creds

]#

if(first()|dupefinished()){
 #horz and vert res
 #color
 #setup array    
 #freq
 # include mapfunction
}
#interval(Frequency)

#Curframe = Curframe % HorzRes
#TableValues[Curframe, number] = GraphicalValuable

#Curframe++

##loop through and display the stuff on egp
